[project]
name = "task-api"
version = "0.1.0"
description = "Simple task management API built with FastAPI."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0"
]